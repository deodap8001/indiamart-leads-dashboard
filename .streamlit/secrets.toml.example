# Streamlit Cloud secrets template.
# For LOCAL development: just use the existing .env file in the project root.
# For Streamlit Community Cloud: paste these keys into the app's "Secrets" UI
# (App settings → Secrets) — do NOT commit a real secrets.toml.

INDIAMART_API_KEY = "your_glusr_crm_key_here"
